faiss-cpu>=1.7.4
numpy>=1.24
pypdf>=4.0.0
python-docx>=1.1.0
scikit-learn>=1.3.0
gradio>=4.30.0
python-dotenv>=1.0.0

# Optional, heavier backends - the app runs fine without them (falls back to
# TF-IDF embeddings + extractive answers). Comment out to keep install light.
sentence-transformers>=2.2.2
openai>=1.0.0
anthropic>=0.34.0
