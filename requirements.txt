faiss-cpu>=1.7.4
numpy>=1.24
pypdf>=4.0.0
python-docx>=1.1.0
scikit-learn>=1.3.0
gradio>=4.30.0
python-dotenv>=1.0.0

# Optional: PyTorch & sentence-transformers are ~4GB with CUDA dependencies.
# Commented out for lightweight cloud hosting (Render/Koyeb 512MB free tier).
# Un-comment if you have a high-memory server with PyTorch installed.
# sentence-transformers>=2.2.2
openai>=1.0.0
anthropic>=0.34.0
